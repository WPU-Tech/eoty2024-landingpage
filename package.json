{
  "scripts": {
    "build": "rm -rf dist && mkdir dist && tailwindcss -i src/style.css -o dist/style.css --minify && cp index.html dist/index.html && sed -i 's|/css/style.css|style.css|' dist/index.html && cp -r asset dist/asset"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
